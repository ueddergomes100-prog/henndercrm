<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version may have breaking changes — APIs, conventions, and file structure can differ from prior Next.js releases.

Before writing Next.js code:

1. Read the relevant guide in `node_modules/next/dist/docs/` when that directory exists.
2. If the installed package does not include that directory, consult the official documentation matching the version declared in `package.json`.
3. Confirm behavior against the installed package types and heed deprecation notices.
<!-- END:nextjs-agent-rules -->
